# 🌦 Weather App GUI

A simple and interactive **Java Swing Weather App** that displays live weather information using the OpenWeatherMap API.

## 🧠 Features
- Shows temperature, humidity, wind speed, and condition  
- Uses real-time weather data from OpenWeatherMap  
- User-friendly Swing interface  

## 🖥️ How to Run
1. Make sure you have Java installed.  
2. Download `json-simple` library (required for JSON parsing).  
3. Compile:
   ```bash
   javac -cp .;json-simple-1.1.1.jar WeatherAppGUI.java
